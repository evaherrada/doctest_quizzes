# This function should should output a number one greater than its input
def addone(num):
    """
      >>> addone(1)
      2
      >>> addone(2)
      3
      >>> addone(0)
      1
      >>> addone(-1)
      0
    """
    ___
